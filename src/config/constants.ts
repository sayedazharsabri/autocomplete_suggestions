export const CONSTANTS = {
    // Number of attempts allowed for any ip in duration of 60(default) seconds.
    IP_RATE_LIMITING_MAX_ALLOWED_ATTEMPTS: 5,

    // Duration in seconds to apply rate limiting over any ip
    IP_RATE_LIMITING_TIME_DURATION: 60
}